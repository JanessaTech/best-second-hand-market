import * as commonConfig from './config.common'
const config = {...commonConfig}

config.BACKEND_ADDR = "http://localhost:3100"
config.SignInEth = 'Sign in with Ethereum to CHEAP written by JanessaTech. Email:janessatech.web3@gmail.com'

export default config